import React from 'react';
import ReactDOM from 'react-dom/client';
import DelmoccaTool from './App';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <DelmoccaTool />
  </React.StrictMode>
);